# 다음에 할 일

## 1. 🗺️ 지도 버튼 검색 실패 — 미해결 (2026-08-08 확인)

**증상**: 일부 장소에서 🗺️ 를 누르면 구글지도가
「Google 지도에서 ○○을(를) 찾을 수 없습니다」 를 띄운다. 휴대폰·PC 둘 다.

**원인**: URL 형식이 아니라 **검색어(`q` 값)** 가 구글지도에서 매칭되지 않는 것.
도시명을 어떤 형태로 붙여도 실패하고 이름 단독만 되는 경우가 있어, 규칙을 세울 수 없다.

나슈마르크트 실측 결과:

| 검색어 | 결과 |
|---|---|
| `Naschmarkt Vienna` ← 현재 앱에 든 값 | ❌ 찾을 수 없음 |
| `Naschmarkt Wien` | ❌ 찾을 수 없음 |
| `Naschmarkt, Wien, Austria` | ❌ 찾을 수 없음 |
| `Naschmarkt` | ✅ 시장 (평점 3.6 · 976개) |
| `48.19818,16.36262` (좌표) | ✅ 정확한 자리 · 단 상점 정보 없음 |

**⚠️ 검증 방법 주의 — 이게 핵심이다**

Nominatim(OpenStreetMap)으로 하는 검사로는 **이 문제가 잡히지 않는다.**
실제로 구글지도 URL을 열어서 「찾을 수 없습니다」가 뜨는지 눈으로 봐야 한다.
`curl` 로도 판별 못 한다 — 페이지가 JS 렌더링이라 성공·실패 HTML이 똑같다.
HTTP 200 은 검증이 아니다(구글은 결과가 없어도 200을 준다).

**미확인**: 나머지 90곳 중 몇 곳이 같은 문제인지 아직 모른다. 전수 확인 필요.

**선택지** (셋 중 하나 고르면 됨)

1. **🗺️ 를 좌표로 전환** — `?api=1&query=<위도>,<경도>`
   91곳 전부 즉시 해결, 검색 실패가 원천적으로 사라짐. 유지보수 불필요.
   대신 영업시간·평점 카드가 안 뜬다. 좌표는 이미 91곳 전부 검증돼 있어 바로 적용 가능.
2. **식당·카페 60곳만 이름 검증, 나머지는 좌표** — 영업시간이 중요한 쪽만 이름을 살린다. 가장 실용적이지만 시간이 걸린다.
3. **91곳 전부 이름 검증** — 가장 오래 걸리고, 구글이 매칭 규칙을 바꾸면 다시 깨질 수 있다.

**그동안의 우회 방법**: 🧭 길찾기와 📌 좌표는 **이름이 아니라 좌표로 열려서 항상 정확하다.**

---

## 이미 확인 끝난 것 (다시 안 해도 됨)

- **좌표 91곳 전부 검증 완료** — Nominatim 조회 + 도시 경계 대조. 좌표 없는 곳 0.
- **실제 구글지도에서 정확히 열리는 것 확인한 4곳**
  · 칸티나 `50.08350,14.42892` · 브 자티시 `50.08444,14.41640`
  · 뤼스빔 `47.50123,19.03300` · 마이슬 & 샤든 `48.20223,16.37493`
- **동명 업소 문제 해결** — 프라하 4구에 같은 이름이 있어 5~7km 어긋나던 2곳.
  검색어에 거리명을 넣어 구분했다(`Kantýna Politických vězňů 5 Praha`,
  `V Zátiší Liliová 1 Praha`).
- **URL 형식** — 구글 공식 `?api=1&query=` 만 쓴다.
  `/@위도,경도,17z` 형식은 비공식이라 앱에서 동작이 보장되지 않는다. 되돌렸음(263752e).

## 여행 전 별도로 확인할 것

- 출발 2~3주 전 **식당·카페 운영시간·휴무 전부 재확인** (홈페이지·구글지도).
  앱에 든 내용은 조사 시점 2026-08 기준이다.
- 12/24 · 12/25 · 1/1 예약을 먼저 확정. 이 세 날이 휴무가 가장 많다.
